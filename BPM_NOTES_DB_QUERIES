PROCESS APP WISE INSTANCE STATUS AND COUNT
 """ SELECT lp.NAME, code.NAME, COUNT(lbi.EXECUTION_STATUS) AS "Count" 
     FROM LSW_BPD_INSTANCE lbi
     LEFT OUTER JOIN LSW_BPD_STATUS_CODES code ON code.STATUS_ID = lbi.EXECUTION_STATUS 
     LEFT OUTER JOIN LSW_PROJECT lp ON lbi.PROJECT_ID = lp.PROJECT_ID 
     GROUP BY lp.NAME, code.NAME 
     ORDER BY lp.NAME, code.NAME WITH ur; """
